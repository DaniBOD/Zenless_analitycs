# Seguimiento de Desarrollo — Fase 2 (Hitos 2.0 → 2.8)

> **Periodo cubierto:** 2026-05-04 → 2026-05-15
> **Total commits:** 45 (+6 en sesiones 2026-05-14/15)
> **Repositorio:** [DaniBOD/Zenless_analitycs](https://github.com/DaniBOD/Zenless_analitycs)
> **Rama de trabajo:** `main` (directo, sin worktree a partir de ahora)
> **Última build:** `1d1ac74` (2026-05-14)

---

## 0. Resumen ejecutivo

En 9 días se cerró el grueso de la Fase 2 del [Roadmap_Motor_Captura_Scoring.md](../Roadmap_Implementacion/Roadmap_Motor_Captura_Scoring.md):

| Fase | Estado | Commits clave |
|------|--------|--------------|
| **Hito 2.0** — ETL saneamiento | ✅ Cerrada | `b5ac31f`, `6bc3bae`, `3a7098d`, `e442ef5` |
| **Hito 2.1-2.2** — Scaffold + scoring engine | ✅ Cerrada | `63f17dc` |
| **Hito 2.3** — Batch scoring 334 discos | ✅ Cerrada | `e442ef5` |
| **Hito 2.4** — OCR + detector + monitor | ✅ Cerrada + 14 iteraciones QA | `847e47b` + 13 fixes |
| **Hito 2.5** — Sync RF-04/05 + hotkeys | ✅ Cerrada | `2089e05` |
| **Hito 2.6** — Optimizador RF-06 | ✅ Cerrada | `dd30857` |
| **Hito 2.7** — UI capturador (RF-11) | ✅ Cerrada | `a718002`, `582308d` |
| **Hito 2.8** — Build .exe + QA real + S18 parser | ✅ Cerrada (sesión 2026-05-13) | `dcd37ce`, `e6f133f`, `3856464` |

**Métricas DB (post-Hito 2.0):**
- `agents`: 46 (incluye Cissia v2.7)
- `inventory_discs`: 334 (332 originales + 2 nuevos)
- `agent_thresholds`: 108
- `pj_weapon_synergy`: 276
- Migraciones aplicadas: 9 (`01` hasta `09_optimizer_pending_actions`)

**Métricas detector (post-sesión 2026-05-14):**
- Estados clasificables: 18 (S1-S18)
- Templates en bundle: **17**
- Tests unitarios: **148/148 verdes** (+40 state machine, temporal buffer, thresholds, slot, FP regression, +17 S18 parser agent stats, +3 structural)
- Slot detection: S17 multi-método (OCR + glow HSV), S9 OCR
- S18 stat extraction: `AgentStatsParsed` con 11 atributos base + nombre + rol + elemento + fuerza_bruta, 23 ROIs calibradas, dual-mode PaddleOCR full-frame+regex / Tesseract per-ROI
- S18 detection: 4 indicadores multi-capa (tab amarillo, grilla stats, valores brillantes, AGENT INFO text) — solo como verificación post-template, no como detector independiente
- TemporalBuffer: majority voting 2/3 sobre 3 frames, fast capture 100ms
- Heartbeat: 5s → **2s**
- Fuerza Bruta como campo propio (solo para disruptivos)
- Nangong Yu corregido: Anomalía → Aturdimiento (migración en DB)
- Dark frame filter restringido al 60% central, dark_ratio 0.50→0.60

---

## 1. Hito 2.0 — ETL de saneamiento (Fase 2.0)

**Objetivo:** dejar la DB consistente antes de empezar a codear core.

### Commits

| Commit | Fecha | Descripción |
|--------|-------|-------------|
| `b4351d1` | 2026-05-04 | **initial commit** — Fase 1 cerrada + roadmap Fase 2 + onboarding Cissia pendiente |
| `b5ac31f` | 2026-05-04 | **onboarding Cissia v2.7** (46/46 agentes) + 2 discos nuevos. Hito 2.0.6. |
| `6bc3bae` | 2026-05-04 | **`audit_inventory_discs.py`** + reporte firmado. Hito 2.0.1. |
| `3a7098d` | 2026-05-04 | **Migración 07 + re-estandarización 334 discos**. Hitos 2.0.3-2.0.4. |
| `e442ef5` | 2026-05-04 | **Seed `substat_preferences`** desde Prydwen + batch scoring 334 discos. Hitos 2.0.5 + 2.3. |

### Outcomes

- DB consistente: typeof homogéneo en `val1-val4` (todos REAL), strings canonicalizados.
- 334 discos scoreados y firmados con `confianza_global`.
- Audit en `audit/inventory_discs_audit_20260504.md`.

---

## 2. Hito 2.1-2.2 — Scaffold `app/` + Scoring Engine puro

| Commit | Fecha | Descripción |
|--------|-------|-------------|
| `63f17dc` | 2026-05-04 | **scaffold `app/`** + `stats_vocab.py` + `scoring.py` + ventana PySide6 base. Fases 2.0-2.2 simultáneas. |

### Outcomes

- Layout previsto del roadmap §3 implementado: `app/core/`, `app/db/`, `app/ui/`, `app/scripts/`, `app/tests/`.
- `stats_vocab.py`: canon de nombres de mainstats/substats con aliases y parser.
- `scoring.py`: engine puro (sin DB, sin UI) — input dict, output score 0-1.
- `recommender.py`: decisión 4-vías (Equipar / Mejorar / Reserva / Descartar) con thresholds defaults 0.75 / 0.50.

---

## 3. Hito 2.4 — Capa OCR + Detector + Monitor (RF-04)

**Esta fue la más compleja**. El commit base creó toda la infraestructura; luego vinieron **14 iteraciones de QA y bugfix** sobre cliente real entre `88363a8` y `9563b80`.

### Commit base

| Commit | Fecha | Descripción |
|--------|-------|-------------|
| `847e47b` | 2026-05-04 | **`feat(hito-2.4)`** — `ocr_backend.py` + `ocr_tesseract.py` + `detector.py` + `capturer.py` + `parser_disc.py` + `monitor.py`. |

### Iteraciones de QA (2026-05-11 → 2026-05-12)

Cada commit corresponde a un hallazgo en QA con cliente real:

| Commit | Fecha | Hallazgo / Fix |
|--------|-------|----------------|
| `88363a8` | 2026-05-11 01:48 | calibración ROIs por estado + state mapping en parser/monitor |
| `3e535ef` | 2026-05-11 01:48 | templates S7 (tienda música) + S11 (desmontaje) |
| `b4d38a5` | 2026-05-11 12:54 | recalibrar ROIs vía OCR inverso + mejorar preprocessing |
| `e132ac1` | 2026-05-11 13:10 | DB writable en `%LOCALAPPDATA%` + auto-start del monitor al detectar ZZZ |
| `a040940` | 2026-05-11 13:25 | logging detallado del detector + threshold 0.85 → 0.70 (luego revertido) |
| `e9275d2` | 2026-05-11 13:46 | diagnostic logging + `find_zzz_window` con substring matching |
| `905211a` | 2026-05-11 14:39 | **filtrar ventana ZZZ por NOMBRE DEL PROCESO**, no por título (importante: el título cambia con la zona del juego) |
| `3cc922f` | 2026-05-11 15:01 | threshold a 0.85 final + log estado-por-categoría + ROI S7 distintivo |
| `f1d35f3` | 2026-05-11 18:45 | **templates S13/S14/S15** para pantallas no-capturables (anti-FP) |
| `40e2ca7` | 2026-05-11 19:20 | S16 anti-FP modal 'Info conjunto' + dedup captura por transición + re-mapear S9 a inventario general |
| `db96ca8` | 2026-05-11 19:39 | templates S3/S9 distintivos + S17 vista detalle PJ (anti-FP raíz) |
| `c5d3951` | 2026-05-11 20:31 | S16 ROI ampliada + nuevo S18 perfil agente (2 variantes: s18a recomendación, s18b equipamiento completo) |
| `bd27442` | 2026-05-11 20:33 | **merge PR #1 → main** (primer push visible para el usuario) |
| `45715de` | 2026-05-11 20:46 | **detección slot 1-6 dentro de S17** vía OCR del título "Set Name (N)" — 12/12 |
| `bd03676` | 2026-05-12 10:52 | heartbeat 15s → 5s + cadencias agresivas (S13/S14/S15/S17/S18 a 1-1.5s) |
| `c43020c` | 2026-05-12 11:11 | **s18c trigger universal** tab 'Atributos base' subrayado amarillo (cubre todas las variantes de Agent Info) |
| `9563b80` | 2026-05-12 11:39 | **detección slot 1-6 en S9** (inventario con disco seleccionado) + fix S3 FP (re-template "DISCO" bottom-right del modal post-farmeo) |

### Outcomes acumulados de Hito 2.4

**Taxonomía final de estados (18 estados, antes 12):**

| Estado | Descripción | Captura? | Slot? |
|--------|------------|----------|-------|
| S1 | Patrulla / menú | ❌ | – |
| S2 | Resultado del desafío | ❌ | – |
| **S3** | Modal detalle disco (post-farmeo) | ✅ | – |
| S4 | Tienda música — selector | ❌ | – |
| S5 | Resultado de afinación | ❌ | – |
| **S6** | Tienda música — panel detalle | ✅ | – |
| **S7** | Tienda música — fullscreen | ✅ | – |
| S8 | Equipamiento disco personaje (preview) | ❌ | – |
| **S9** | Inventario general de discos | ❌ + slot via OCR | ✅ (1-6 con disco seleccionado) |
| **S10** | Modal upgrade disco | UPGRADE | – |
| S11 | Pantalla desmontaje | ❌ | – |
| S12 | Sin coincidencia (transición) | ❌ | – |
| S13 | Selección set farmeo | ❌ | – |
| S14 | Selección equipo pre-combate | ❌ | – |
| S15 | Menú personajes | ❌ | – |
| S16 | Detalle set de discos (modal Info conjunto) | ❌ | – |
| **S17** | Detalle disco PJ (Personalización pistas) | ❌ + slot via OCR | ✅ (1-6) |
| S18 | Perfil agente — Atributos base | ❌ | – |

**Lecciones aprendidas del proceso QA:**

1. **Templates basados en texturas genéricas → FP garantizado.** Resolución: usar SIEMPRE elementos con texto distintivo (footers, headers, breadcrumbs, etiquetas únicas).
2. **El threshold 0.85 es el sweet spot**: 0.70 produce FPs con elementos UI repetidos entre pantallas; 0.85 con templates específicos da TP > 0.95 y FP < 0.6.
3. **Heartbeats demasiado esparcidos (15s) ocultan el comportamiento del detector** en transiciones rápidas. 5s + cadencias por estado (1-1.5s en pantallas intermedias) es lo correcto.
4. **Dedup por transición ≠ dedup por tiempo**: capturar UNA vez al entrar a un disc-state (S3/S6/S7), resetear flag al salir.
5. **OCR del título "Set Name (N)" + regex `r"\((\d)\)"` con psm=6 es robusto** (12/12 S17, 6/6 S9).
6. **Templates de un mismo estado pueden ser múltiples** (S18 tiene s18a/s18b/s18c — variantes mutuamente excluyentes o redundantes).

---

## 4. Hito 2.5 — Sync RF-04/05 + Hotkeys

| Commit | Fecha | Descripción |
|--------|-------|-------------|
| `2089e05` | 2026-05-05 | **`feat(hito-2.5)`** — sync RF-04 (`sync_equip.py`) + RF-05 (`sync_upgrade.py`) + hotkeys F8 (force scan) / F10 (pause) + UpgradeSyncer S10 PRE/POST. |

### Outcomes

- F8 fuerza scan inmediato; F10 pausa/reanuda monitor.
- UpgradeSyncer detecta entrada a S10, captura snapshot PRE, compara con POST al salir.
- Hook `EVENT_SYSTEM_FOREGROUND` win32 para forzar scan cuando ZZZ vuelve al frente.

---

## 5. Hito 2.6 — Optimizador RF-06

| Commit | Fecha | Descripción |
|--------|-------|-------------|
| `dd30857` | 2026-05-05 | **`feat(hito-2.6)`** — `optimizer.py` (greedy + bonus pass) + migración 09 (`optimizer_pending_actions`) + auto-trigger desde RF-04. |
| `d3a733d` | 2026-05-05 | docs: actualizar `project-context-IA.md` — Fase 2 cerrada inicialmente, 9 migraciones aplicadas. |

### Outcomes

- Optimizador build greedy con bonus pass — encuentra mejor disco por slot, luego verifica bonus de set 2pc/4pc.
- Tabla `optimizer_pending_actions` registra Equipar/Mejorar/Reserva/Descartar.
- Auto-trigger: cada disco capturado por RF-04 dispara recálculo del PJ afectado.

---

## 6. Hito 2.7 — UI Capturador (RF-11)

| Commit | Fecha | Descripción |
|--------|-------|-------------|
| `a718002` | 2026-05-11 01:48 | **`asset_resolver`** para set logos + avatares (Hito 2.7) |
| `582308d` | 2026-05-11 01:49 | **`feat(rf-11)`**: UI capturador completa — tokens + toast + live_panel + controller |
| `fdbc81c` | 2026-05-11 01:49 | **PyInstaller spec --onedir** + shortcut script + E2E offline tool |
| `3cdabbf` | 2026-05-11 01:49 | docs: Guía QA del domingo + project-context actualizado |
| `7175aac` | 2026-05-11 01:55 | empaquetar assets de Hito 2.7 en el .exe + actualizar Guía QA |
| `a68b524` | 2026-05-11 13:57 | **fix(build)**: empaquetar todos los submódulos de mss en el .exe |
| `6a4f6e5` | 2026-05-11 14:16 | **fix(build)**: usar `collect_all()` para mss/PySide6/pytesseract/pynput (bundle 804 MB) |

### Outcomes

- Toast flotante 380×116 px implementado.
- Panel principal `live_panel` con log de pantallas + estado del monitor.
- Tokens CSS-in-Qt para paleta Claude Design.
- `asset_resolver.py` resuelve logos de set y avatares desde el bundle.
- .exe `--onedir` empaquetado en `dist/DaniBOD_ZZZ_Analytics/` (804 MB).
- Shortcut launcher en `tools/create_shortcut.ps1`.

---

---
# SECCIÓN 1: Detector Robusto + S18 Stats (sesión 2026-05-13)

Todo el contenido de esta sección corresponde a los commits de las sesiones del 13-14 de mayo:
`f9342c1` → `dcd37ce` → `e6f133f` → `3856464` → `37cf49c` → `297441e` → `75494c8` → `78a0afd`

## 7. Hito 2.8 — QA final + S18 Perfil agente

**Estado: ✅ Cerrada**

### 7.1 TemporalBuffer — majority voting multi-frame

Nuevo `TemporalBuffer` en `detector.py` con ventana de 3 frames. Solo emite un cambio de estado cuando 2/3 frames coinciden (majority vote). Integrado en `monitor.py._run()`:
- Captura rápida cada **100ms** (10 fps)
- Procesamiento pesado (OCR, parseo) solo cuando el buffer confirma el estado
- Elimina FPs transitorios por ruido de un solo frame

### 7.2 Heartbeat 2s (antes 5s)

El heartbeat de diagnóstico pasa de cada 5s a cada **2s** para feedback más rápido.

### 7.3 S18 detection reforzada — 4 indicadores multi-capa

Además de los 3 templates (s18a/s18b/s18c), se agregaron **4 indicadores de verificación** (cualquiera basta para clasificar S18):

| # | Indicador | Método | Robustez |
|---|-----------|--------|----------|
| 1 | Subrayado amarillo del tab "Atributos base" | HSV mask | Alta (color único) |
| 2 | Grilla de stats (+4 líneas paralelas) | Canny + HoughLinesP | Alta (layout único) |
| 3 | Valores brillantes columna derecha | Bright pixel ratio | Media (depende de contraste) |
| 4 | **"AGENT INFO"** texto sutil | CLAHE + adaptive threshold | **Alta** (exclusivo de S18) |

### 7.4 S18 stat extraction — `parser_agent_stats.py`

Nuevo parser que extrae los **11 atributos base** del personaje desde la pantalla S18:

**Layout de columnas (confirmado por DaniBOD):**
```
Columna izquierda           Columna derecha
─────────────────────       ────────────────────
Nivel (Nv. 60)
PV                         Ataque
Defensa                    Impacto
Probabilidad de CRIT       Daño CRIT
Tasa de Anomalía           Maestría de Anomalía
Tasa de Perforación        Recuperación de Energía
```

**Archivos creados:**
| Archivo | Propósito |
|---------|-----------|
| `app/core/parser_agent_stats.py` | `AgentStatsParsed` + `parse_agent_stats(frame, ocr)` |
| `app/config/rois.toml` » `[perfil_agente_atributos]` | 23 ROIs calibradas (11 labels + 11 valores + AGENT INFO) |
| `app/core/detector.py` » `AGENT_STATS_STATES` | S18 movido de `NON_CAPTURE_STATES` a nuevo set |
| `app/core/monitor.py` » `on_agent_stats` callback | Dispatch a `_maybe_process_agent_stats` en S18 |

### 7.5 Dark frame filter fix

`_is_dark_frame` restringido al **60% central** del ancho (x=0.20 a x=0.80) para evitar que personajes con fondo oscuro en los bordes disparen el filtro en S18. `dark_ratio` subido a 0.60.

**Antes:** S18 clasificaba como S12 por dark frame filter (fondo oscuro del personaje >50%).
**Después:** S18 detectado correctamente en 7/7 screenshots.

### 7.6 Pruebas con screenshots reales

| Screenshot | Agente | Detección | Indicadores |
|-----------|--------|-----------|-------------|
| 1 | Nangong | ✅ S18 (conf=1.0) | `agent_info_text` |
| 2 | — | ✅ S18 (conf=1.0) | `agent_info_text` |
| 3 | — | ✅ S18 (conf=1.0) | `agent_info_text` |
| 4 | — | ✅ S18 (conf=1.0) | `agent_info_text` |
| 5 | — | ✅ S18 (conf=1.0) | `agent_info_text` |
| 6 | — | ✅ S18 (conf=1.0) | `agent_info_text` |
| **7** | **Cissia** | ✅ **S18 (conf=1.0)** | **`tab_amarillo+agent_info_text`** |

Los 7 screenshots se copiaron a `app/tests/fixtures/` como fixtures de test.

### 7.7 Tests

| Test | Resultado | Descripción |
|------|-----------|-------------|
| `test_agent_stats_parsed_fields` | ✅ | 11 campos + confianza + notas |
| `test_parse_agent_stats_returns_defaults_with_empty_ocr` | ✅ | Frame vacío → todos None |
| `test_all_rois_have_text_content` | ✅ | 23/23 ROIs con texto en 7 capturas |
| `test_extracts_all_11_stats[1-7]` (PaddleOCR) | ⏭️ Skipped | OneDNN incompatible en este equipo |
| `test_extracts_some_stats_with_tesseract[1-7]` | ⏭️ Skipped | Tesseract binario no instalado |
| **Total suite** | **134 passed, 14 skipped** | |

### 7.8 Nota técnica: instalación de PaddleOCR

En este equipo (Python 3.11 Windows Store) PaddleOCR no pudo instalarse completamente por:
1. **MAX_PATH** (260 chars) — solucionado instalando en `D:\paddle_site`
2. **OneDNN incompatible** — PaddlePaddle 3.3.1 + PaddleOCR 2.10.0: `fused_conv2d` no soportado

En la máquina del usuario (instalación Python normal, no Windows Store), PaddleOCR debe funcionar sin problemas con `pip install paddlepaddle==2.6.2 paddleocr==2.8.2`.

### Pendientes de la sesión

- [ ] Probar S17/S18 en juego real (flujo completo: S8 → S17 → S18 → S8)
- [ ] Instalar PaddleOCR compatible en máquina del usuario → 7 tests de extracción pasan con 11/11 stats
- [ ] Reducir bundle size (804 MB → < 200 MB)

### Hallazgo post-sesión: S18 no detectado en juego real

Ver **Sección 12** para diagnóstico completo. Resumen: las 3 templates S18 no matchean 2560×1440 del usuario, y `_verify_s18` solo corre como confirmación post-template, no como detector independiente. Se requiere detector multi-trigger delegado.

---
# SECCIÓN 2: Seguimiento Desarrollo Continuo

## 8. Pendientes conocidos (global)

1. **S3 FP modal recompensa diaria** — al abrir el juego, el modal de "Recompensa sesión diaria" disparó S3 conf 0.88. Sin screenshot del modal específico no se puede tightener template sin riesgo. Pendiente repro con captura.
2. **S18 no detectado en juego real** — 3 templates S18 no matchean 2560×1440. `_verify_s18` solo corre como confirmación post-template. Se requiere detector multi-trigger independiente (ver Sección 12).
3. **Bundle size 804 MB** — `collect_all('PySide6')` trae todo PySide6. Optimización post-QA.
4. **Soporte resoluciones < 1920×1080** — deferido. Templates calibrados a 2559×1439 (escalado por matchTemplate).
4. **Fase 3 (futuro)** — RF-12 sinergias IA + lookup determinista, RF-13 lategame F11.

---

## 9. Convenciones operativas adoptadas durante el desarrollo

### Git workflow (decisión del usuario 2026-05-11)

- Trabajo en worktree `claude/mystifying-proskuriakova-4dc8ce`.
- **Push directo a `main`** con `git push origin HEAD:main` después de cada commit relevante.
- No se crean PRs (excepto PR #1 inicial). Justificación: único colaborador, quiere ver avances en tiempo real.
- Ver memoria: `~/.claude/projects/.../memory/feedback_git_main.md`.

### Layout hexagonal de slots (decisión del usuario 2026-05-12)

```
Slot 1 (arriba-izq)    Slot 6 (arriba-der)
Slot 2 (medio-izq)     Slot 5 (medio-der)
Slot 3 (abajo-izq)     Slot 4 (abajo-der)
```

**No es como Genshin.** ZZZ va: columna izquierda 1→2→3 descendente, columna derecha 6→5→4 descendente. Documentado en `memory/zzz_slot_hexagon_layout.md`.

### Heartbeats y cadencias (QA 2026-05-12)

- Heartbeat global cada **5s** (antes 15s).
- Polling cadence por estado (ms):
  - Disc states (S3/S6/S7): 500
  - Upgrade (S10): 500
  - Intermedios (S13/S14/S15/S17): 1000
  - S9/S16/S18: 1500
  - S8: 1500
  - S2/S5: 1000
  - S12 (no reconocida): 2000
  - S1/S4: 4000
  - S11: 5000

---

## 10. Próximos pasos planeados

1. ~~**Implementar deep S18 multi-trigger**~~ — ✅ cerrado 2026-05-15. Ver [`2026-05-15_Hito_2.8_Deep_S18_Multi_Trigger.md`](2026-05-15_Hito_2.8_Deep_S18_Multi_Trigger.md).
2. ~~**Si el multi-trigger no alcanza: recapturar templates S18**~~ — deferido, no necesario: el deep_detect es resolución-agnóstico y cubre el caso.
3. **Instalar PaddleOCR en máquina del usuario** (`pip install paddlepaddle==2.6.2 paddleocr==2.8.2`) → 7 tests S18 con 11/11 stats extraídos.
4. **Probar flujo S17/S18 en juego real** — S8 → S17 (slot detection + parsing disco equipado) → S18 (stat extraction). **Bloqueante para cierre Hito 2.8.**
5. **Bundle size** — reducir de 804 MB a < 200 MB removiendo PySide6 submodules no usados.
6. **Fase 3 (futuro)** — RF-12 sinergias IA + lookup determinista, RF-13 lategame F11.

---

## 11. Archivos de referencia

- **Roadmap canónico:** [`Roadmap_Implementacion/Roadmap_Motor_Captura_Scoring.md`](../Roadmap_Implementacion/Roadmap_Motor_Captura_Scoring.md)
- **Contexto IA:** [`../../project-context-IA.md`](../../project-context-IA.md)
- **RF base:** [`RF_Captura_Discos/RF-Logic_Captura_Discos.md`](../RF_Captura_Discos/RF-Logic_Captura_Discos.md)
- **QA plan:** [`QA/QA-00_Plan_Maestro.md`](../QA/QA-00_Plan_Maestro.md)
- **CLAUDE.md operativo:** [`../../CLAUDE.md`](../../CLAUDE.md)

---

---
# SECCIÓN 3: Sesión 2026-05-14/15 — Controller wiring + QA en juego real

---

## 12. Conexión controller + QA S18 en juego real

### 12.1 Controller wiring — on_agent_stats callback

| Commit | Fecha | Descripción |
|--------|-------|-------------|
| `1d1ac74` | 2026-05-14 | feat(ui): conectar on_agent_stats callback + log stats S18 en .exe |

**Cambios en `app/ui/controller.py`:**
- Se agregó `on_agent_stats=self._on_agent_stats_from_monitor` al constructor del Monitor (línea 108).
- Nuevo handler `_on_agent_stats_from_monitor` que parsea `AgentStatsParsed` y emite:
  - `log.info()` detallado con los 11 stats + nombre/rol/elemento
  - `agent_stats_detected` Signal (dict) para futura UI
  - `log_message` con resumen legible: `[stats] Yixuan PV=17245 ATK=1907 DEF=667 CR=65% CD=162%`

### 12.2 QA en juego real — resultado del test

El usuario ejecutó el .exe y navegó desde el mundo abierto hasta el perfil de un personaje:

```
09:38:11  [diag] ventana ZZZ encontrada: 'ZenlessZoneZero [zenlesszonezero.exe]' (2560x1440)
09:38:12  [pantalla] no reconocida — posible menú/transición (s13, conf=0.51)
09:38:28  [diag] heartbeat: 4 ticks, último estado=S12 (conf 0.58)
09:38:43  [diag] heartbeat: 7 ticks, último estado=S12 (conf 0.59)
...
09:40:17  [pantalla] S15 — Menú de personajes (plan de entrenamiento) · sin disco visible (conf 1.00)
09:40:30  [pantalla] no reconocida — posible menú/transición (s13, conf=0.52)
09:40:35  [diag] heartbeat: 29 ticks, último estado=S12 (conf 0.52)
```

| Aspecto | Resultado |
|---------|-----------|
| Ventana detectada | ✅ 2560×1440, nombre de proceso correcto |
| Heartbeat | ✅ Correcto, muestra S12 iterativamente |
| S15 (Menú personajes) | ✅ Detectado conf 1.00 desde "Plan de entrenamiento" |
| S18 (Perfil agente) | ❌ **No detectado** — muestra "pantalla no reconocida" al navegar al detalle de personaje |
| Mejor match parcial | S13 (conf 0.52) — ninguna template S18 superó threshold 0.75 |

### 12.3 Diagnóstico — por qué falla S18 en juego real

**Problema arquitectónico en `detector.py`:**

```
classify():
  1. _template_match() → S12 (ninguna template S18 superó threshold 0.75)
  2. _verify() → no ejecuta nada (saltado porque state.code == "S12")
  3. _classify_by_hsv() → grid lines no detectadas → S12
  ⚠️ _verify_s18 JAMÁS se ejecuta porque solo corre como confirmación
     de un template match exitoso, no como detector independiente
```

**Causas raíz:**

1. **Templates no matchean** — las 3 templates S18 son crops pequeños (562×31, 460×57, 435×86 px) que no corresponden con el renderizado 2560×1440 del usuario. Posiblemente capturadas de una resolución/estado de UI diferente.

2. **`_verify_s18` es reactiva, no activa** — la función con los 4 indicadores (tab amarillo, grilla stats, valores brillantes, AGENT INFO) solo se invoca desde `_verify()` cuando `state.code != "S12"`, es decir, solo cuando template matching YA produjo S18. No puede detectar S18 por sí misma.

3. **El HSV fallback chequea grid solamente** — `_classify_by_hsv` para S18 usa Canny + HoughLinesP, que no detectó las líneas de la grilla de stats del usuario (parámetros no calibrados para su resolución).

4. **Sin OCR en el detector** — el `ScreenDetector` no tiene acceso al OCR backend, por lo que no puede verificar "AGENT INFO" textual ni buscar keywords de stats en la capa de clasificación.

### 12.4 Solución propuesta — Deep S18 multi-trigger (delegada)

Se diseñó pero **no se implementó en esta sesión**. Se delega a otro desarrollador.

**Idea general:** crear una función `_deep_detect_s18(frame, ocr) → ScreenState | None` que:

1. Corre como fallback en `monitor.py` después de `detector.classify()` cuando el resultado es S12
2. Evalúa **5 indicadores independientes** sin requerir template matching:

| # | Indicador | Método | Peso |
|---|-----------|--------|------|
| 1 | Subrayado amarillo del tab "Atributos base" | HSV inRange (ya existe en `_verify_s18`) | 1 |
| 2 | Grilla de stats (≥4 líneas paralelas) | Canny + HoughLinesP (ya existe, recalibrar parámetros) | 1 |
| 3 | Valores brillantes columna derecha | Bright pixel ratio (ya existe) | 1 |
| 4 | Texto "AGENT INFO" sobre nombre de facción | **OCR Tesseract** sobre ROI dedicada (nuevo) | **2** |
| 5 | Keywords de stats (PV, ATK, DEF, PROB. CRIT, etc.) | **OCR Tesseract** sobre panel de stats (nuevo) | **2** |

3. Score ≥ 3 → S18 confirmado (confianza 0.75+). Score == 2 → S18 tentativo (confianza 0.55).
4. La función vive en `monitor.py` donde OCR está disponible, o bien se pasa OCR al detector.

**Ubicación propuesta en `monitor.py._process_frame()`:**
```python
state = self._detector.classify(frame)
if state.code == "S12":
    s18_state = _deep_detect_s18(frame, self._ocr)
    if s18_state is not None:
        state = s18_state
```

### 12.5 Pendientes de esta sesión

- [x] **Implementar deep S18 multi-trigger** (función independiente de templates) — cerrado 2026-05-15 (ver [`2026-05-15_Hito_2.8_Deep_S18_Multi_Trigger.md`](2026-05-15_Hito_2.8_Deep_S18_Multi_Trigger.md))
- [ ] Recapturar templates S18 desde máquina del usuario a 2560×1440 — **deferido**, no es necesario: el deep_detect cubre el caso sin atar a resolución específica
- [x] **Cierre del gap test↔runtime** — agregados 40 tests (27 unit + 13 integration) que cubren el pipeline real `classify→deep_detect→buffer→dispatch→callback`. Suite: 148 → 188 tests verdes
- [ ] **QA en juego real** del flujo completo S15 → seleccionar PJ → S18 → ver log `[stats]` — pendiente bloqueante para cerrar Hito 2.8

---

*Doc actualizado 2026-05-15 al cierre de la sesión de QA en juego real (commit `1d1ac74`). Mantener actualizado al cierre de cada hito significativo de la Fase 2.8 (QA real) y al iniciar Fase 3.*
