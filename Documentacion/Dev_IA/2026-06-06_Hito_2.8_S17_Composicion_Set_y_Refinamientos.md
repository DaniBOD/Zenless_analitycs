# Hito 2.8 — Sesión 2026-06-06 (b): Composición de set por PJ + tier activo + refinamientos de captura S17

> **Sesión:** 2026-06-06 (segunda tanda del día; continúa la sesión `(a)` documentada en
> `2026-06-06_Hito_2.8_Regresion_Build_Env_PaddleOCR.md` §9)
> **Branch base:** `main`
> **Owner:** DaniBOD (con asistencia IA)
> **Scope:** Sobre la base de la captura S17 (PJ asignado por avatar + bono de conjunto), agregar
> tres cosas que pidió el usuario en vivo: (1) detectar si el set tiene **2pc o 4pc ACTIVO**, (2)
> derivar la **composición de set completa del PJ** (4+2 estándar / exótica / incompleta) desde las
> capturas S17, y (3) mostrar los **rolls de cada substat** en el log. Más dos fixes que salieron de
> la verificación en vivo: badge de rolls envuelto y `rebuild.ps1` abortando por stderr.

---

## 0. TL;DR

Partiendo de la captura S17 ya funcional (disco + slot + set + bono + PJ asignado), esta tanda
cerró lo que faltaba para **entender la build de un PJ desde sus discos equipados**:

- **Tier activo (2pc/4pc) por color de texto** — el juego pinta en **blanco** el tier activo y en
  **gris** el inactivo. Se lee el p95 de brillo del prefijo `"N pistas:"` (umbral 190; medido
  blanco=255 / gris=142). Antes se volcaba la descripción completa del 4pc al log (sobraba).
- **Composición de set del PJ derivada de S17** — agrupando los discos equipados del PJ por
  `set_id` se obtiene la composición exacta (`4pc X + 2pc Y · 6/6 · estándar 4+2`, o `exótica`
  como 2+2+2, o `incompleta` con slots faltantes). **Validado en datos reales:** Nangong Yu →
  `{Jazz Caótico: 2, Melodía de Faetón: 4}`, coincide con el hexágono.
- **Rolls en el log de substats** — formato `DC=9.6% (+1)` pedido por el usuario.
- **Fix: substats no leídos (la ñ y el texto chico)** — `Daño Crítico` no canonizaba (el OCR
  mangléa la `ñ`: `Dao`/`Daio`/`Dauo`), el badge `+N` de nombres largos (`Probabilidad de Crítico`)
  se perdía, y valores de un dígito (`Maestría = 9`) se dropeaban. **No eran las tildes.** Resuelto
  con fallback difuso para la ñ + rescate por re-OCR upscaleado para badge y valor. 12/12 capturas
  limpias.
- **Fix CRÍTICO: `find_by_hash` corrompía discos entre PJs** — la clave de dedup
  `(set, slot, main, mainval)` colisiona en el 72% de los discos del roster; `persist_s17_disc`
  pisaba/robaba el disco de otro PJ. Reescrito a clave natural `(PJ, slot)`; sin PJ confiable no
  persiste. DB de la app restaurada desde el seed limpio (RNF-01).
- **Fix: `rebuild.ps1` abortaba por stderr** — PyInstaller escribe su log INFO a stderr; con
  `$ErrorActionPreference='Stop'` la primera línea mataba el build en PS 5.1. Relajado localmente.

Suite total **366 verde** (+32 desde la tanda `(a)`; incluye captura visualización de grilla +
optimización RNF-06). `.exe` reconstruido desde el `.venv`
(33.67 MB, determinista) y verificado en vivo por el usuario.

---

## 1. Tier de conjunto ACTIVO (2pc/4pc) — por color de texto

**Pedido del usuario:** "en el 4pc extraes toda la descripción del set, sobra de momento; quiero
ver que el sistema reconozca si tiene activo 2 o 4 pc". Dos señales visibles: (a) el color del
texto del bono (blanco=activo / gris=inactivo), (b) los iconos de los discos.

**Decisión:** se eligió (a) **color de texto** — es local al frame S17 ya capturado, sin tocar S8
ni template-matching de iconos.

**Cómo:** en "Efecto de conjunto", el bloque `"N pistas:"` del tier **activo** está en blanco
(el 2pc siempre activo; el 4pc blanco si hay 4+ piezas, gris si solo 2-3). Se muestrea el **p95 de
brillo** del prefijo `"4 pistas:"` (35% izquierdo de la línea, antes de keywords coloreadas):
`> 190 → 4pc activo`, si no `2pc`. Medido sobre 8 capturas reales: **activo p95≈255, inactivo≈142**
→ umbral 190 con margen enorme.

- `parser_disc_s17.py::detect_active_set_tier(frame, lines, W, H) -> int | None`
- `DiscParsed.set_active_tier: int | None` (None si no se localiza la línea "4 pistas:")
- Se computa en `parse_disc_s17_full` y se loguea como `[conjunto] <set> · tier activo: 4pc`.

> **Commit:** `a44cbf4`.

---

## 2. Composición de set del PJ — derivada de capturas S17

**Pedido del usuario:** pre-capturar la composición 2+2 / 4+2 / nulos / exóticas desde la pantalla
de equipamiento general (S8), donde se ven los 6 discos.

**Feasibility (medido, read-only) → descartado el camino S8-icono:** el icono in-game (marco
oscuro + hub central + emblema en anillo) es estructuralmente distinto del logo del wiki (emblema
lleno); el match da basura (Jazz→"Proto_Punk" 0.63). Calibrar las 6 posiciones del hexágono en S8
+ un descriptor robusto no es un ajuste chico y su confiabilidad estaba sin validar.

**Decisión del usuario:** **derivar la composición de las capturas S17** (confiable, reusa lo
hecho). Cada disco equipado capturado en S17 ya guarda `set_id` (OCR del título, fiable), `slot` y
`agente_asignado`. Agrupar por `set_id` los discos equipados del PJ da la composición exacta.

**Implementación** (`sync_equip.py`, módulo-nivel):
- `compute_set_composition(con, agente_id) -> dict`:
  ```sql
  SELECT iv.slot, iv.set_id, ds.nombre
  FROM inventory_discs iv LEFT JOIN disc_sets ds ON ds.id = iv.set_id
  WHERE iv.agente_asignado = ? AND iv.equipado = 1
    AND (iv.descartado = 0 OR iv.descartado IS NULL)
  ```
  Devuelve por-set `{nombre, count, slots, tier}` (tier: ≥4→4pc, ≥2→2pc, ==1→suelto), `slots_llenos`,
  `faltantes = {1..6} − llenos`, y `clasificacion`: **estándar 4+2** (counts == [4,2], 0 faltantes) /
  **exótica** (otra combinación completa, p.ej. 2+2+2) / **incompleta** (hay slots faltantes →
  nulos o no capturados aún).
- `format_composition(comp) -> str`: resumen legible
  `"4pc Melodía de Faetón + 2pc Jazz Caótico · 6/6 · estándar 4+2"`.
- `SyncResult.set_composition: str | None`; se computa en `persist_s17_disc` tras el upsert (con la
  conexión del syncer, `check_same_thread=False` — corre en el thread del monitor, **no** se
  reintrodujo el bug de threading de la tanda `(a)`).
- Log `[composición]` en `controller._log_s17_extraction`, tras `[persistido]`. Se va completando a
  medida que se parsean los 6 slots (muestra faltantes/nulos en el camino).

**Validado en DB real:** Nangong Yu (id=26) → 6 discos → `{Jazz Caótico: 2, Melodía de Faetón: 4}`
→ coincide con el screenshot (2 amarillos + 4 morados).

**Alcance / honestidad (RNF-02):** desde S17 **no** se distingue con certeza *nulo real* vs *no
capturado aún* (un slot faltante puede ser cualquiera de los dos). Se reporta como
`"faltante (nulo/no capturado)"`. La detección de **nulo visual** requeriría el camino S8-icono
(descartado). No se inventa.

> **Commit:** `463b47c`. **Camino S8-icono** queda documentado como alternativa futura si se quiere
> el pre-capture visual con detección de nulos real (clustering de iconos + calibración de las 6
> posiciones del hexágono en S8).

---

## 3. Rolls de cada substat en el log

**Pedido:** "en el log de las sub stats añade entre paréntesis al lado de cada stat numérico sus
rolls de mejora, ej: `DC=9.6 (+1)`".

`controller._fmt_sub` ahora emite `{nombre}{val}{roll}` con `roll = " (+N)"` cuando `rolls > 0`.

> **Commit:** `a44cbf4` (junto con el tier activo).

---

## 4. Fix — badge de rolls envuelto (nombre de substat largo)

**Bug reportado en vivo** (Melodía de Faetón slot 5, Nangong Yu):

```
[disco] subs: Daño Crítico 9.6% (+1) · Maestría de Anomalía 27 (+2) · Prob. Crítica 4.8% · ATK 19
                                                                      ↑ falta el (+1)
```

**Causa:** cuando el nombre del substat es largo (`Probabilidad de Crítico`), el juego **parte el
badge `+1` a una línea aparte** debajo del nombre. PaddleOCR lo devuelve como una línea suelta en
la columna de nombre. El parser S17 saca los rolls del texto del nombre (`_split_rolls`), así que
leía `"Probabilidad de Crítico"` con rolls=0 y el `"+1"` quedaba huérfano (substat fantasma).

**Fix** (`parser_disc_s17.py::_coalesce_rolls_fragments`): antes de emparejar nombre↔valor, se
fusionan los badges `"+N"` sueltos (regex `^\+?\s*([0-5])$`, seguro porque en la columna de nombre
no hay tokens de un solo dígito) con la línea de nombre **inmediatamente superior** en la misma
columna (guarda de adyacencia vertical ≤ 2× alto de línea). Tras fusionar, `_split_rolls` recupera
el roll correcto y el valor empareja limpio con el único nombre restante de esa fila.

**Tests:** `test_parser_disc_s17.py::test_rolls_badge_envuelto_se_fusiona` (reproduce las líneas
OCR con bbox del caso real) + `test_coalesce_no_toca_nombres_normales` (identidad sin badges
envueltos). Verificado en vivo: el `(+1)` ahora aparece en `Prob. Crítica`.

---

## 4b. Fix — substats no leídos: la 'ñ' y los textos chicos dropeados por OCR

La verificación en vivo del fix anterior destapó **dos misses de OCR distintos** en los
substats, ambos confirmados sobre las 12 capturas reales de
`Screenshots_Triggers/Discos_Triggers/14_Slots_equipamiento/`:

**(a) La 'ñ' de "Daño Crítico" → `substat_desconocido`.** El OCR latino mangléa la `ñ` de forma
**inconsistente** (`Daño` → `Dano`/`Dafo`/`Daio`/`Dauo`/`Dao` según el frame). Había alias fijos
para `Dano` (ñ→n) y `Dafo` (ñ→f), pero no escalan a cada variante. **Importante:** **no eran las
tildes** — las tildes (í, á, ó) el OCR las dropea limpio (`í`→`i`) y `_norm_key` ya las ignora;
la `ñ` es el único carácter que el OCR convierte en *otra letra*.
- **Fix:** fallback **difuso** en `stats_vocab.normalize_stat_name` (stdlib `difflib`): si la clave
  exacta falla, busca el canónico más cercano con `cutoff=0.84`. **Guarda de ambigüedad** (RNF-02):
  se abstiene (None) si hay otro canónico distinto casi igual de cerca (margen 0.06) — esto evita
  confundir `Maestría de Anomalía`↔`Tasa de Anomalía` (ratio 0.81). Las variantes `X`/`X%` cuentan
  como misma base (el parser corrige el `%` por la unidad del valor). Todos los mangles de la ñ
  resuelven a `Daño Crítico` (ratio 0.87–0.95).

**(b) Texto chico dropeado: badge "+N" envuelto y valores de un dígito.** PaddleOCR downscalea el
frame a 960px (`det_limit_side_len`); el texto chico aislado cae bajo el umbral del detector y
**desaparece** del OCR. Dos manifestaciones:
- El badge `+N` de un nombre largo (`Probabilidad de Crítico`) se **envuelve** a una 2ª línea
  naranja → no se detecta → roll se leía 0. (Mi primer intento, `_coalesce_rolls_fragments`, era
  inútil: **no hay** fragmento `"+1"` que fusionar; el badge no está en el OCR.)
- El **valor** de un substat (p.ej. `Maestría de Anomalía = 9`) se dropea por ser un dígito chico
  → `valor=None`. **No es la tilde** del nombre — es el dígito.
- **Fix:** **rescate por re-OCR upscaleado** en `parser_disc_s17`. Para cada substat con `rolls==0`
  (`_rescue_wrapped_roll`) o `valor is None` (`_rescue_missing_value`), se recorta la zona puntual
  (banda bajo el nombre para el badge; columna de valor a la derecha del nombre para el valor),
  se **upscalea ×3** y se re-OCRiza. El badge lleva **gate por color naranja** (frac ≥ 0.015) para
  no re-OCRizar substats blancos con 0 rolls genuinos. Validado: el `9` que full-res da `[]` se lee
  `9` conf 1.0 upscaleado; el `+1` se recupera conf 1.0.
- `_coalesce_rolls_fragments` se conserva (cubre el caso —posible en otras resoluciones— en que el
  badge **sí** se detecta pero como línea aparte).

**Resultado sobre las 12 capturas:** **0 discos con problemas** (antes: nombres `desconocido` por la
ñ + varios `valor=None` + rolls envueltos perdidos). Tests: `test_stats_vocab` (+3 difuso) y
`test_parser_disc_s17` (+4 e2e con Paddle vivo: rescate de badge y valor, ñ, invariantes).

---

## 4c. 🐛🐛 Fix CRÍTICO — `find_by_hash` corrompía discos entre PJs

El QA en vivo (parseo de Nangong Yu + Dialyn) destapó un **bug de corrupción de datos
pre-existente** (venía de la asignación S17, commit `39e6a0f` — NO de los cambios de esta tanda).

**Síntomas en el log:** `[persistido] disco id=7` al capturar el Jazz slot1 de Nangong (cuyo disco
real es id=43), y una composición transitoria imposible (`4pc Jazz + 4pc Melodía · 6/6 · exótica`
= 8 piezas en 6 slots).

**Causa raíz:** `InventoryDiscRepo.find_by_hash` deduplica por **`(set_id, slot, main_stat,
main_valor)`** — **sin substats ni PJ**. En el roster real, **239 de 334 discos (72%)** comparten
esa firma con discos de OTROS PJs (74 grupos de colisión, hasta 6 PJs cada uno; p.ej.
`(Jazz, slot1, HP, 2200)` la tienen 5 PJs: Burnice, +otros). `persist_s17_disc` usaba esa clave →
`find_by_hash` devolvía el disco de id más bajo (de otro PJ), y `update_from_parsed` le **pisaba los
substats** + `update_assignment` lo **reasignaba**.

**Daño probado** (diff backup-de-sesión vs DB actual): el disco **id=7 (de Burnice)** apareció con
los **stats de Nangong Yu + `agente_asignado=26`**, y el disco **id=335 (de Nangong) desapareció**.

**Fix** (`repositories.py` + `sync_equip.py`):
- Nueva clave NATURAL del equipamiento: **`find_equipped_by_agent_slot(agente_id, slot)`** — un PJ
  tiene exactamente un disco equipado por slot; nunca devuelve el de otro PJ.
- `persist_s17_disc` reescrito:
  - **Sin PJ confiable ⇒ NO se persiste** (antes insertaba/matcheaba a ciegas). El disco S17 está
    equipado en *algún* PJ; sin saber cuál, cualquier match por firma colisiona. Se loguea y se
    abstiene (RNF-02). El usuario identifica el PJ (Atributos base) y re-captura.
  - Con PJ: busca el disco `(PJ, slot)`. Mismo set ⇒ `update` (refresco nivel/substats). Set
    distinto ⇒ **swap** (des-equipa el viejo, inserta el nuevo). Sin disco previo ⇒ **alta**.
- `find_by_hash` se conserva para `on_disc_detected` (flujo de disco farmeado S3/S6/S7, sin PJ — ahí
  el dedup por firma es el comportamiento buscado).

**Saneo de datos (RNF-01):** la user-data DB del `.exe`
(`%LOCALAPPDATA%/DaniBOD_ZZZ_Analytics/db/`) quedó churneada por las sesiones buggy de hoy → se
**restauró desde el seed limpio** del repo (`db/danibod_zzz_v2.db`, integrity ok, 335 discos, 0
duplicados PJ+slot), con backup previo de la corrupta (`*.backup_precorrupt_*`). El seed es la fuente
de verdad (las curaciones del usuario viven ahí; el persist es de hoy y todo lo que escribió estaba
corrupto).

**Tests:** `test_s17_avatar_assignment` — `test_persist_s17_no_colisiona_entre_pjs` (regresión
directa: capturar el disco del PJ B no toca el de A con misma firma), `test_persist_s17_update_mismo_pj_slot`
(no duplica), `test_persist_s17_sin_pj_confiable_no_persiste`, `test_persist_s17_sin_pj_no_pisa_lo_curado`.

---

## 4d. Captura en modo VISUALIZACIÓN de la grilla S17 (2026-06-07, display-only)

El usuario pidió que, al **navegar la grilla de discos candidatos** en S17 (seleccionar, no
equipar), el sistema capture el disco resaltado: extraer stats + detectar **equipado/no y por qué
PJ**. Sirve a futuro para el flujo de equipamiento de discos. Ejemplos:
`04_Inventario_Disco_Vista_Individual/Ejemplo_11_nangong.png` (no equipado) y `Ejemplo_12` (equipado
por otro PJ).

- **Re-captura por FIRMA del disco** (`monitor.py`): antes el dedup era por `(code, slot)`, así que
  navegar candidatos del mismo slot no re-capturaba. Ahora `_s17_disc_signature` (downscale 12×12
  gris de la banda de detalle) + `_s17_update_and_check_capture` re-capturan cuando el disco cambia
  (cambio de slot O de candidato), filtrando la transición con N frames estables. `_S17_SIG_DIFF_MAX=10`.
- **Estado de equipamiento (display)**: `DiscParsed.equip_detectado/equip_pj_visual`, seteados en
  `_assign_s17_pj` (independiente de la guarda de persistencia). Avatar presente ⇒ equipado
  (`AgentIdentifier.identify_s17` nombra el PJ, aunque sea otro ≠ latch); ausente ⇒ no equipado.
  Log `[equipamiento] equipado por X / NO equipado (disco disponible)` en el controller.
- **Fix detección de avatar — saturación → bordes.** `_has_avatar_content` usaba saturación y daba
  **falso positivo** en la grilla: el arte de fondo borroso está MÁS saturado que un avatar (medido:
  fondo sin-avatar 0.38 > avatar 0.31). Se cambió a **densidad de bordes (Canny)**: el avatar
  (anillo + cara) tiene bordes nítidos (equipados reales 0.10–0.14), el fondo liso casi ninguno
  (0.000). Umbral 0.04, margen enorme.
- **Riesgo de corrupción nulo:** navegar la grilla NO persiste (la guarda abstiene en candidatos de
  otros PJs / sin avatar; el persist requiere PJ confiable). Es puramente aditivo (extracción + log).
- Tests: detección equipado/no en Ej_11/Ej_12 (e2e Paddle), re-captura por firma, campos de
  visualización en `_assign_s17_pj`. Suite **366 verde**.

---

## 4e. Optimización de responsividad S17 (RNF-06, 2026-06-07)

El usuario reportó que la extracción S17 tardaba **4-6s por disco** (viola RNF-06). Desglose medido
(offline, `parse_disc_s17_full` sobre captura real):

- **OCR del parse ~2s:** 1 llamada Paddle sobre el frame completo 2559×1439 = **1206ms** + 2 rescates
  (re-OCR de badge/valor: 310ms + 492ms).
- **Pre-OCR ~2-3s (el desperdicio):** `extract_s17_slot` OCReaba el **título del slot en CADA frame**
  durante la estabilización (3 frames), y como cada OCR de slot frena el loop, no corría a 10fps.

**Fix (`monitor.py`):** la **firma visual del disco** (hash 12×12 gris del panel, BARATO, sin OCR) ya
gobierna la captura → se eliminó el OCR de slot por-frame; el slot se OCRea **una sola vez al capturar**
(`if s17_capture_ready: raw_state.slot = extract_s17_slot(...)`). El loop corre a 10fps real →
estabilización ~0.3s en vez de ~3s.

**Resultado esperado: ~4-6s → ~2.3s** (≈2×). El piso restante (~2s) es la inferencia de PaddleOCR; bajarlo
más requiere un OCR más liviano o un crop del panel (medido: crop = 1.7s pero elimina los rescates) — queda
como trabajo futuro. Cosmético: el `[pantalla] S17` ahora muestra `slot None` hasta capturar (el slot se lee
del título al parsear).

---

## 4f. 🔑 Hallazgo de observabilidad — el SANDBOX bloquea LOCALAPPDATA (2026-06-07)

Durante el QA del fix del persist apareció un fantasma: el `id=7` + `4pc+4pc exótica` **reaparecía en cada
sesión** pese a restaurar la DB desde el seed limpio una y otra vez. Causa raíz descubierta:

**Los comandos del agente (Bash/PowerShell) corren en un SANDBOX que intercepta las escrituras fuera del
repo.** La DB del `.exe` vive en `%LOCALAPPDATA%/DaniBOD_ZZZ_Analytics/db/` → **todos los `Copy-Item` de
restore fueron al overlay del sandbox, nunca a la DB real que lee la app.** El agente "veía" una DB limpia
(su overlay) mientras la app seguía con la DB real corrupta (duplicados de sesiones viejas).

- Los **builds** (a `app/build/dist/`, dentro del repo) SÍ llegan al FS real → por eso los rebuilds del
  `.exe` sí tomaban efecto, pero los restores de DB no.
- Para tocar la DB real hay que pasar **`dangerouslyDisableSandbox: true`** (y verificar bien, hubo
  inconsistencias de vista entre overlay y real).
- **Implicancia:** el agente está parcialmente **ciego al estado real de la app**, lo que rompe el ciclo de
  QA de cualquier cosa que toque la DB.

**Solución IMPLEMENTADA (2026-06-07): override `DANIBOD_DB_PATH`.** En
`app/db/connection.py::_resolve_db_path` se agregó un override por env var que **gana sobre todo lo demás**
(antes de la rama frozen/dev): si `DANIBOD_DB_PATH` está seteada (no vacía), la app —incluso el `.exe`— abre
ESE path tal cual, sin copiar nada. Soporta `~`. Para el QA del `.exe` se lo apunta a la DB del repo
(`db/danibod_zzz_v2.db`) lanzando el `.exe` con la env var seteada en la sesión, de modo que el agente lea/
arregle/verifique **la misma DB** que usa la app, sin pelear con el sandbox. Tests:
`test_connection_db_path.py` (override gana, vacío se ignora, expande `~`, e2e `get_connection`).

**Nota:** el fix del persist en sí es correcto (probado en vivo: slot 6 → `id=48` cuando no hay duplicado).
El `id=7` persistente es **data vieja corrupta en la DB real**, no un bug del código nuevo — pendiente de
limpieza SQL sobre la DB real (RNF-01) o de la solución de observabilidad.

---

## 4g. Logging EDGE-triggered (2026-06-07) — del log continuo al gatillado por cambio

El QA en vivo dejó claro que el logging era ruidoso: heartbeat cada 2s + stats de agente
(S18) y detalle (S8/S19) re-logueados en **cada ciclo de cadencia**. Servía al principio (ver
persistencia); con el sistema maduro el usuario pidió **gatillado por cambio**: cada categoría
loguea **1 vez** y re-loguea **solo cuando cambia el dato** (caso explícito: extracción
parcial→completa debe mostrar ambos logs).

- **Heartbeat → eliminado.** Se quitó el bloque periódico (`_HEARTBEAT_S`, `last_heartbeat`). El
  cambio de estado, que era `log.debug`, se **promovió a INFO** en `_notify_state_change`:
  `[estado] <prev> → <code>[ slot=N] (conf=..)`. Es edge por naturaleza (solo se llama al cambiar
  code/slot).
- **Agent stats (S18) → edge en el controller.** `_on_agent_stats_from_monitor` calcula una firma
  `(nombre, 11 stats, tuple(missing))` (sin `conf`, que fluctúa) y emite el `log.info("Stats
  agente …")` + las 3 líneas UI (`[reconocido]/[stats]/[completo]|[parcial]`) **solo si cambió**.
  Un parcial que se completa cambia `missing`/valores ⇒ re-loguea (requisito). El **binding del
  panel** (`agent_stats_detected`) se emite SIEMPRE (no es log). La firma se resetea en
  `_on_state_from_monitor` (cualquier cambio de estado) → re-entrar a Atributos base loguea 1 vez.
  El **procesamiento** sigue continuo (el aggregator madura los parciales); solo la emisión es edge.
- **Detalle S8/S19 → edge en el monitor.** `_process_agent_detail_continuous` gatea por firma
  `(code, name, identified, source)` en `_last_detail_sig`; se resetea en `_reset_detail_identity`.
- Los marcadores per-ciclo del monitor (`[S18] Extrayendo stats…` y `Stats agente (post-merge)…`)
  pasaron a `log.debug` (diagnóstico interno; el log user-facing lo emite el controller, edge).

**Tests:** `test_controller_graceful::test_agent_stats_log_edge_triggered` (idéntico no re-emite;
cambio de stat/parcial→completo sí; `conf` no gatilla; reset por cambio de estado) +
`test_monitor_s18_dispatch::test_agent_detail_edge_emite_una_vez_hasta_cambio` (reescrito del viejo
"re-emite cada dispatch").

---

## 4h. Captura de slots del MISMO set — firma HÍBRIDA (2026-06-07)

**Bug (confirmado en vivo):** navegar entre slots del **mismo set** (p.ej. Melodía slot 4→5) **no
re-capturaba**; solo disparaba al cambiar de **set**. Causa: `_s17_disc_signature` usaba un crop
12×12 gris del panel donde título + labels (idénticos en mismo set) dominaban y las diferencias de
main/substats se promediaban bajo el umbral (10).

**Fix — firma híbrida de 2 componentes con OR** (`monitor.py`):
- `sig_detail`: 48×48 gris del bloque **main + 4 substats** (`x∈[0.30,0.52] y∈[0.22,0.56]`),
  excluyendo título/nivel/labels para no diluir → captura lo que SÍ difiere entre discos del mismo
  set (también cubre navegación de candidatos en la grilla).
- `sig_hex`: 24×24 gris del **hexágono** (`x∈[0.58,0.95] y∈[0.18,0.88]`) — el anillo de selección
  se mueve al cambiar de slot.
- "Disco nuevo" si **cualquiera** supera su umbral (`_S17_SIG_DETAIL_MAX=5.0`,
  `_S17_SIG_HEX_MAX=3.0`).

**Calibración** sobre `14_Slots_equipamiento/` (slots 1-6 reales): con esos umbrales TODOS los
cambios de slot superan el umbral, incluidos los adyacentes del mismo set (peor caso 4↔5 ≈ 1.6× el
umbral); frame idéntico = 0. Re-capturar el mismo disco es **idempotente** (update in-place), así
que se sesga a sensibilidad; el piso real de ruido se afina en el QA en vivo. **RNF-06:** ambas
componentes son recorte + `cv2.resize` + `abs().mean()`, sin OCR — costo despreciable a 10fps; el
slot se OCRea solo al capturar (no se tocó §4e).

**Tests:** `test_s17_avatar_assignment::test_s17_recaptura_slots_mismo_set_adyacentes`
(4→5, 5→6, 2→3 con capturas reales).

---

## 4i. `_resolve_set_id` insensible a tildes (2026-06-07)

**Bug (QA):** el slot 2 no persistió — OCR leyó `"Melodia de Faetón"` (sin la í) y
`_resolve_set_id` (case-insensitive pero **acento-sensible**) no matcheó `"Melodía de Faetón"`
→ `set desconocido — no se persiste`. Jazz (1,6) zafó porque el OCR mantuvo la ó.

**Fix** (`sync_equip.py`): el fuzzy ahora normaliza ambos lados con `_norm_key` de `stats_vocab`
(NFD + quita `Mn` + minúscula + sin espacios — el mismo normalizador que resuelve la ñ de los
substats) y matchea por igualdad/substring normalizados. Subsume el viejo substring lowercase.

**Tests:** `test_s17_avatar_assignment::{test_resolve_set_id_sin_tilde,
test_resolve_set_id_no_inventa_set, test_resolve_set_id_distingue_sets_parecidos}`.

---

## 4j. Fase 1 — S17 CONTINUO + `DiscAggregator` (2026-06-07, captura confiable)

**Dolor (QA en vivo, readonly):** captura S17 lenta (10s+) y **poco confiable** — slots que no se
tomaban hasta "mover y volver", y `slot=0`. Causa de raíz del "mover y volver": S17 era **one-shot**
(1 firma estable → 1 OCR pesado + rescates → final), así que un **frame malo** obligaba a re-navegar.

**Insight (replicar S18):** S18 OCRea el frame completo igual, pero es responsivo porque es
**continuo + `AgentStatsAggregator`**: re-extrae cada cadencia y **fusiona parciales** (mejor valor
conocido por campo; reset al cambiar de agente) → converge en pocos ciclos sin necesitar un frame
perfecto. Se trasladó ese modelo a S17.

- **S17 pasa a estado CONTINUO** (`_maybe_process_disc` → `_process_disc_s17_continuous`). La **firma
  híbrida** (§4h) deja de ser el gatillo de captura y pasa a ser el **detector de cambio de disco**
  (resetea el aggregator), igual que S18 resetea por cambio de agente.
- **`DiscAggregator`** (`parser_disc_s17.py`, espejo de `AgentStatsAggregator`): fusiona `DiscParsed`
  parciales del mismo disco — escalares adoptan el valor nuevo válido si no conservan; substats por
  índice (gana el frame que leyó el valor); `confianza_global` = máximo. `reset()` lo dispara el
  monitor por cambio de firma.
- **Madurez antes de emitir** (`disc_is_mature`: set resoluble + slot 1-6 + main_valor + ≥4 substats
  con valor y nombre): se emite (`_on_disc`/persist) al **madurar** o tras `_S17_AGG_MAX_CYCLES=5`
  best-effort. Esto elimina la dependencia de un frame perfecto → **mata el "mover y volver"** y
  mitiga `slot=0` (conserva el mejor slot no-cero entre ciclos).
- Frames de transición (`confianza_global < 0.7`) **no** contaminan el aggregator.
- **F8** resetea el tracking para forzar re-emisión del best-known.

**Tests:** `test_s17_avatar_assignment::{test_disc_aggregator_converge_parciales,
test_s17_continuo_emite_una_vez_al_madurar, test_s17_continuo_baja_confianza_no_contamina,
test_maybe_process_disc_s17_va_al_handler_continuo}` + `test_disc_is_mature`.

---

## 4k. Fix QA — re-emisión del mismo disco por parpadeo de firma (dedup por identidad)

**Bug (QA en vivo 21:19, readonly):** con Fase 1 la responsividad mejoró mucho (cada slot en
`agg 1c`, sin mover-y-volver), **pero** el MISMO disco estático se re-emitía ~7× en ~33s
(`Disco detectado slot=1 (agg 1c)` repetido). El log `[S17] asignado…` también salía **cada ciclo**.

**Causa:** la firma híbrida (§4h) es el gatillo de reset del aggregator, y su componente `sig_hex`
abarca la **región derecha con el modelo 3D del disco/PJ, que tiene animación idle** → el diff medio
cruza `_S17_SIG_HEX_MAX` en pantalla estática → falso "disco nuevo" → reset + re-madura en 1 ciclo →
re-emite. Tensión inherente: el umbral debe ser sensible para distinguir slots adyacentes del mismo
set (§4h), y esa misma sensibilidad dispara falsos resets en estático. En live el efecto es un
re-`UPDATE` idempotente de la misma fila `(PJ,slot)` (no corrompe, pero es ruido y **regresión** vs.
el one-shot viejo que emitía 1×).

**Fix (`monitor.py`), sin tocar umbrales** (no arriesga §4h):
- **Dedup por IDENTIDAD de emisión:** set `_disc_emitted_ids` con `(set, slot, main)` ya emitidos en
  la sesión S17, **normalizados con `_norm_key`** (sin tildes/mojibake). Al madurar, si la identidad ya
  se emitió → **no** re-emite (ni re-persiste), aunque la firma haya parpadeado. La firma sigue
  reseteando el aggregator (sirve para *cambiar* de disco); el set se limpia al **salir de S17**
  (`_reset_s17_disc_tracking`) y con **F8**. *(Normalización agregada 2026-06-08 — regresión QA Fase 2:
  el OCR del crop lee la tilde del set inestable entre ciclos, "Faetón"→"Faeton", y sin normalizar
  re-emitía el mismo disco. Test `test_s17_dedup_identidad_insensible_a_mojibake_de_tilde`.)*
- **Log `[S17] asignado` edge-triggered:** helper `_log_s17_assign(sig, …)` con `_s17_assign_sig`;
  loguea 1× por firma de decisión `(estado, latch[, pj_visual])` y re-loguea al transicionar
  (equipado ↔ otro-PJ ↔ sin-avatar). Alinea el log de asignación con el resto del edge-logging (§4g).

**Tests:** `test_s17_avatar_assignment::{test_s17_continuo_no_re_emite_por_parpadeo_de_firma
(firma que parpadea cada ciclo → emite 1×; otra identidad → re-emite; reset → re-emite),
test_log_s17_assign_edge_triggered}`.

---

## 4l. Fase 2 — OCR sobre CROP nativo del panel (latencia)

**Problema:** cada ciclo de cadencia (`S17 = 1000 ms`) corría `text_with_bboxes` sobre el **frame
completo 2560×1440**. El detector DBNet **reduce a ≤960 px** (`det_limit_side_len`) → el texto chico
(badges `+N`, valores de 1 dígito) se cae → dispara rescates (re-OCR). OCR de 3.7 MP + rescates.

**Fix (`parser_disc_s17.py`):** OCR sobre un **crop nativo del panel** y RE-OFFSET de cada bbox a
coords de frame completo, así el parser espacial (banda, headers, avatar, tier) **no cambia**.
- `_S17_DETAIL_PANEL_ROI = (0.27, 0.105, 0.36, 0.645)` → **922×928 px** (lado mayor < 960 ⇒ sin
  downscale; texto chico en 1ª pasada). Calibrado en **Paso 0** (medición offline read-only sobre
  `14_Slots_equipamiento/` + `04_Inventario.../Ejemplo_{1,8,9,10,11,12}`): abarca título (incl. 2
  líneas), nivel, main, 4 substats y la línea `4 pistas:` (la usa `detect_active_set_tier`, llega a
  y≈0.73). `_ocr_detail_lines(frame, ocr)` con fallback al frame completo si el crop falla.
- **Headers tolerantes:** prefijos `atributoprincip` / `atributossecundari` / `efectodeconjunt` (el
  crop a veces trunca/funde el header, p.ej. "Atributo principal"→"Atributoprincipa"). Sin esto el
  fallo del header cascada a slot=0 + main=None. Beneficia también al path de frame completo.
- **Rescate de slot** (`_rescue_slot_from_title`): el crop a veces pierde el `(N)` del título (dígito
  fino entre paréntesis, sobre todo `(1)`). Cuando `slot==0`, re-OCR de una franja fina del título
  (1-2 líneas) lo recupera de forma fiable.
- **Rescate de rolls unificado** (`_rescue_roll`, reemplaza `_rescue_wrapped_roll`): el badge `+N` se
  cae en dos disposiciones — **inline** (re-OCR de la fila completa del nombre hasta antes del valor;
  cubre que el crop funda el `+N` dentro del bbox del nombre) o **envuelto** (2ª línea, banda más
  generosa, tolera ±px). Gate por color naranja ⇒ solo re-OCRiza si hay badge real (no penaliza
  substats con 0 rolls legítimos). Más robusto que la versión vieja (solo miraba abajo, frágil a px).

**Validación (Paso 0, 18 capturas reales):** **paridad TOTAL** crop vs frame-completo en
slot/main/set/nivel/substats(valor+rolls)/tier; OCR base ~30 % más rápido (≈1.2 s → ≈0.85 s); el piso
restante son los rescates (re-OCR puntual) + el recognizer de Paddle. **RNF-06:** el OCR sigue solo
en la cadencia, fuera del loop rápido 10 fps; el crop reduce trabajo, no agrega.

**Tests:** `test_parser_disc_s17::{test_e2e_crop_slot_correcto (slots 1-6, incl. rescate de `(1)`),
test_rescue_slot_from_title_slot1, test_e2e_crop_tier_detectado, test_ocr_detail_lines_offset_a_frame_completo}`
+ los e2e existentes (`test_e2e_*`) ahora ejercen el path de crop. Script de medición de Paso 0:
throwaway (`tools/_measure_s17_crop.py`), borrado tras calibrar.

---

## 4m. Robustez multi-PJ (2026-06-08, QA Burnice/Yixuan/Caesar)

QA de robustez con discos de **Burnice, Yixuan y Caesar** (capturas `_3/_4/_5` en
`14_Slots_equipamiento/`). Tres hallazgos + sus fixes:

- **🔴 S17 de PJs con fondo oscuro NO se reconocía (se "detectaba S18/S12").** El
  **dark-frame filter** (`detector.py`, Capa 0) corría ANTES del template y tiraba a
  S12 toda pantalla con dark-ratio (centro) > 0.68. Las S17 de Burnice/Caesar miden
  **0.68–0.71** (fondo de elemento oscuro) → filtradas → **no se capturaba nada**;
  Yixuan (0.63) pasaba. **Fix:** el filtro pasa a ser **template-aware** — corre
  DESPUÉS del template/verify y solo gatea a S12 **si ningún template matchó**. Un
  match fuerte de S17 (conf 1.00) manda. Las FP reales (loading 0.24–0.52; o
  `Detalle_set_disco` 0.76 que matchea su propio template **S16**) siguen sin
  capturarse. Validado: 18/18 S17 nuevas → S17; FPs → S16/S2/S12; loading → S12.
  (El glow-detector `_detect_slot_by_glow` está descalibrado para la grilla actual —
  1/18 — su recalibración es Fase 3.)
- **Set con letra dropeada no resolvía.** `'Fábula Yunkui'`→`'Fäbua Yunkui'` (falta la
  'l'): el substring de `_resolve_set_id` no lo capturaba (`fabuayunkui` ⊄
  `fabulayunkui`). **Fix:** fallback **difflib** (cutoff 0.86 + guarda de ambigüedad,
  mismo patrón que la 'ñ') → resuelve `Fäbua`→Fábula (r=0.96) y se abstiene ante lo
  ambiguo (RNF-02).
- **Substat fantasma numérico.** Un fragmento `'12'` (valor/badge mal segmentado) caía
  en la columna de nombre y generaba un 5º substat con `canon=None`. **Fix:** filtrar
  nombres SIN letras tras `_coalesce_rolls_fragments` (un nombre de stat siempre tiene
  letras; los `+N` legítimos ya se fusionaron).

**Tests:** `test_detector_fp_regression::test_s17_fondo_oscuro_se_reconoce` (4 capturas
Burnice/Caesar), `test_s17_avatar_assignment::test_resolve_set_id_fuzzy_drop_de_letra`,
`test_parser_disc_s17::test_e2e_no_substat_fantasma_numerico`. Suite **402**.

**Pendiente (no en esta tanda):**
- **slot=1 → 0** en algunos fondos (Yixuan/Caesar): PaddleOCR dropea el `(1)` (dígito
  fino) en el título Y en la franja de rescate; no recuperable por OCR. **Lo resuelve
  Fase 3** (slot determinista por celda resaltada del hexágono, sin OCR).
- **`Balada de la rama y la espada`** no resuelve: el catálogo guarda el alias corto
  `Balada rama/espada` (id 25) y además existe `Balada de aguas blancas` (id 51) →
  ambiguo para el fuzzy. **Fix de DATOS** (RNF-01): actualizar el `nombre` del set 25
  al nombre largo del juego (requiere confirmación + backup).

---

## 4n. Fase 3 — Slot DETERMINISTA por el hexágono (mata el `slot=0`)

**Problema (QA Fase 2):** el slot salía del OCR del título `'Set (N)'`. PaddleOCR
**dropea el '1' fino de '(1)'** en algunos fondos (Yixuan/Caesar) → `'Set ()'` →
`slot=0` → el disco no maduraba → **no se emitía** (en el QA multi-PJ se perdió el
slot 1 de Yixuan y César). El OCR no lo recupera a ninguna escala (ni la franja).

**Fix (`parser_disc_s17.py`):** `_detect_s17_slot_by_hexagon(frame)` — el hexágono de
equipados (derecha) marca el slot con un **aro dorado**; se mide la fracción de oro en
un **annulus** (el aro, no el relleno del arte del disco) en cada uno de los 6 centros
calibrados y se elige el máximo. **Sin OCR.** Centros por HoughCircles, layout ZZZ
(`zzz_slot_hexagon_layout`): col izq 1→2→3, col der 6→5→4. Abstención si el mejor no
supera piso (0.06) ni le saca margen al 2º (0.04) — frames de transición.
- **Autoritativo:** si el hexágono resuelve con confianza, manda sobre el OCR del
  título (si discrepan, nota `slot_ocrN_vs_hexM`). Cae al OCR del `(N)` y luego al
  rescate de franja (Fase 2) solo si el hexágono se abstiene.

**Validación:** **30/30** capturas reales (slots 1-6 × 5 PJs) — incl. los slot=1 que
fallaban. Margen best-vs-2º real ≥ 0.11 (umbral 0.04). RNF-06: sin OCR, solo máscara
HSV en 6 anillos chicos.

**Tests:** `test_parser_disc_s17::{test_slot_por_hexagono (10 casos),
test_slot_hexagono_se_abstiene_sin_seleccion}`. Suite **413**.

---

## 4o. Fase 4 — Identidad de PJ (avatar best-match, anti-pollución, retroceso S17→S8) + fix Balada

QA multi-PJ mostró asignación de PJ frágil: Nangong rechazaba sus propios discos
(`sim=0.734`), librería de avatares con entradas espurias, y mis-IDs al cambiar de PJ.

- **Anti-pollución del roster (`agent_identifier.py`):** la librería se bootstrapeaba
  con CUALQUIER nombre de OCR de S18 → coló p.ej. `'Permiso'` (no es agente). Ahora
  `learn`/`learn_s17` **validan y canonicalizan** contra el roster (`agents`, vía
  `_norm_key`): nombre fuera del roster → no se aprende; `'Lucia'`→`'Lucía'`.
  `prune_to_roster()` (en el load) elimina entradas espurias (quita `'Permiso'`).
  Roster perezoso desde la DB (respeta `DANIBOD_DB_PATH`); sin DB → no valida (compat).
- **Asignación S17 = CONFIAR EN EL LATCH (`monitor._assign_s17_pj`):** antes un umbral
  único (`sim<0.86 → rechazar`) **falso-rechazaba** a Nangong (descriptor viejo 0.734).
  El primer intento (best-match con `identify_s17` + margen) **falló en QA en vivo**: el
  avatar circular S17 es poco discriminativo — un descriptor "imán" (Yixuan) matcheaba
  ~todo a 0.86-0.91 y rechazaba a Sunna/Zhao/Dialyn/Lucía como "otro PJ 'Yixuan'".
  **Decisión:** el LATCH (identidad de S8/S18 — avatar de fila + OCR, más fiable) es la
  fuente; el avatar circular S17 SOLO decide si re-aprender el descriptor (sim baja o
  ausente → refrescar; self-heal de Nangong), **nunca rechaza**. Para el disco EQUIPADO
  esto es correcto (el avatar es el del latch). La discriminación de discos de OTRO PJ
  (navegar la grilla de candidatos) **se difiere a su fase** (necesita descriptores
  S17 mejores o detectar el modo grilla).
- **Retroceso S17→S8 hereda el PJ (pedido del usuario):** al volver del detalle del
  disco (S17) al hexágono (S8) es el MISMO PJ. `_dispatch_state` detecta la transición
  (`_prev_state_code=="S17"`) y RE-ANCLA el latch a la posición actual del avatar →
  `_update_detail_identity` lo SOSTIENE (heredado) en vez de re-matchear (que
  mis-identificaba al volver).
- **Fix de datos Balada (migración 07):** el catálogo guardaba el alias corto
  `'Balada rama/espada'` (set 25) pero el OCR lee `'Balada de la rama y la espada'` → no
  resolvía (fuzzy se abstenía por el 2º Balada, id 51). Se renombró al nombre completo
  del juego (RNF-01: backup + transacción + `foreign_key_check`/`integrity_check` OK).
  Tras esto el match es EXACTO y no colisiona con id 51. Aplicado a la DB del repo y a
  la de LOCALAPPDATA. `db/migrations/2026-06-08_07_balada_full_name.sql`.

**Limitación conocida (futura fase grilla):** al confiar en el latch, un disco de OTRO
PJ visto navegando la grilla de candidatos se asignaría al PJ actual. Hoy NO es un
problema (el flujo solo ve discos equipados del PJ; y el QA es readonly), pero la fase
de grilla/sustitución debe re-introducir una discriminación FIABLE (descriptores S17
mejores, o detectar el modo grilla vs equipado) antes de persistir en ese flujo.

**Tests:** `test_s17_avatar_assignment::{test_monitor_confia_en_latch_para_disco_equipado,
test_monitor_self_heal_descriptor_viejo_no_falso_rechaza, test_identifier_learn_valida_y_canonicaliza_por_roster,
test_identifier_prune_to_roster, test_retroceso_s17_a_s8_hereda_pj}`. Suite **417**.

---

## 5. Fix — `rebuild.ps1` abortaba por stderr de PyInstaller

**Síntoma:** `tools\rebuild.ps1` cortaba en la **primera línea** del log de PyInstaller
(`402 INFO: PyInstaller: 6.20.0`) sin generar `.exe`.

**Causa:** PyInstaller escribe su log INFO a **stderr**. En PowerShell 5.1, el stderr de un `.exe`
nativo se envuelve como `NativeCommandError` (ErrorRecord); con `$ErrorActionPreference = "Stop"`
(seteado al tope del script) esa primera línea **termina el script** antes de que PyInstaller
corra. No era un fallo de empaquetado — lo abortaba PowerShell.

**Fix:** alrededor de la invocación de PyInstaller se relaja `$ErrorActionPreference = "Continue"`
(guardando/restaurando el valor previo); la corrección sigue gateada por el chequeo explícito de
`$LASTEXITCODE`. El build vía Bash (que no envuelve stderr) ya funcionaba y fue el usado esta
sesión; el parche deja `rebuild.ps1` utilizable de nuevo.

---

## 6. Validación

- **Suite:** `417 passed` (+83 desde la tanda `(a)`: composición, tier activo, wrap de rolls,
  cross-thread, re-captura por slot, fallback difuso de la ñ, rescate e2e de badge y valor,
  no-colisión entre PJs en el persist, override `DANIBOD_DB_PATH`/readonly, logging edge-triggered,
  firma híbrida de slots, tildes en `_resolve_set_id`, S17 continuo + `DiscAggregator`, dedup de
  emisión por identidad, **Fase 2: crop nativo + rescates de slot/rolls + offset**).
- **12/12 capturas S17 reales** (`14_Slots_equipamiento/`): 0 discos con `desconocido`,
  0 valores `None`, todos los rolls totales ≤ 5.
- **`.exe`:** reconstruido desde el `.venv` (guard `Build env: 1.26.4|OK`), **33.67 MB**, sin torch
  → idéntico al baseline determinista. Shortcut del escritorio re-apuntado.
- **En vivo (usuario):** tier activo correcto (`4pc`), composición correcta contra el hexágono,
  rolls en el log, y el `(+1)` de `Prob. Crítica` ya presente tras el fix del wrap.

---

## 7. Lecciones aprendidas

- **Derivar > re-detectar.** La composición de set salió "gratis" de data ya capturada (S17 →
  `inventory_discs`) en vez de pelear con template-matching de iconos en S8. Cuando un dato ya vive
  en la DB, agruparlo es más confiable que re-extraerlo de píxeles.
- **El layout del juego no es estable por fila.** Nombres largos envuelven el badge de rolls a otra
  línea; cualquier parser que asuma "nombre + rolls en una sola línea" se rompe en los stats de
  nombre largo. Coalescing por geometría (columna + adyacencia) lo absorbe.
- **stderr ≠ error en PowerShell 5.1.** Herramientas que loguean a stderr (PyInstaller, muchos CLI)
  abortan scripts con `ErrorActionPreference='Stop'`. Relajar localmente y gatear por
  `$LASTEXITCODE`.
- **Honestidad sobre "nulo".** No se puede afirmar un slot nulo desde S17; reportar
  "faltante (nulo/no capturado)" en vez de inventar (RNF-02).

---

*Documento generado al cierre de la sesión 2026-06-06 (b). La captura S17 ahora entiende la build
del PJ: tier de conjunto activo (2pc/4pc), composición de set completa (estándar/exótica/incompleta)
derivada de los discos equipados, y rolls por substat — robusto a badges de rolls envueltos por
nombres largos. Camino S8-icono diferido como alternativa futura para detección de nulos visual.*
